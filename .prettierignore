.next
out
build
node_modules
pnpm-lock.yaml

# Next.js rewrites these on every dev/build — let it own their formatting
tsconfig.json
next-env.d.ts

# Agent tooling, not app code. The skills are vendored verbatim and tracked by
# hash in skills-lock.json — reformatting them would break that.
.claude
.agents

# Vendored minified build output from pdfjs-dist, copied in verbatim so the
# worker version cannot drift from the library's. Prettier would expand it to
# thousands of lines and the next copy would revert them.
public/pdf.worker.min.mjs

# The medic's discharge corpus is an external input contract, not our source.
# It is the ground-truth label set for extraction QA, so it stays byte-for-byte
# as delivered — and re-sending a corrected letter must not turn CI red.
# Our own generated fixtures/nhs-drug-map.json is NOT ignored and stays checked.
fixtures/discharge-summaries
